"""
srlib — the robotics library you build across this course.

Nothing in here is written for you. Each module is a skeleton: real function
signatures, real docstrings, and a `NotImplementedError` where the maths goes.
You fill them in, one lesson at a time, and by Level 5 you'll be importing
rotation code you wrote in Level 0.

That accumulation is the point. This is a library, not 31 disconnected exercises.

    module          filled in during
    ------          ----------------
    sim.py          L0.1, L0.2
    plotting.py     L0.3
    rotations.py    L0.6, L0.7
    sensors.py      L0.9
    control.py      L0.8, L3.3, L3.4
    filters.py      L2.1, L2.3, L4.2, L4.3, L4.4, L5.3
    robots/         L0.x, L1.1, L3.5, L5.x
"""

__version__ = "0.0.0"  # bump a minor version each level you finish
